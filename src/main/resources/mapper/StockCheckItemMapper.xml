<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ala.stockcheck.mapper.StockCheckItemMapper">

    <insert id="insertItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stock_check_item
            (check_id, product_id, system_qty, real_qty, diff_qty)
        VALUES
            (#{checkId}, #{productId}, #{systemQty}, #{realQty}, #{diffQty})
    </insert>

</mapper>